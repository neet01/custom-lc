CLAUDE.md
# AGENT RULES

- Never introduce external SaaS dependencies
- Prefer AWS-native or self-hosted solutions
- All features must be behind feature flags
- Do not log sensitive data by default
- Always ensure the app builds and runs after changes
- Use migrations for DB changes